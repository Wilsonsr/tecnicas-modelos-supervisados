# Entorno del curso — Técnicas de Análisis Estadístico de Modelos Supervisados
# Instalación:  pip install -r requirements.txt
#
# Los mínimos no son decorativos: por debajo de ellos el código no corre.
#   scikit-learn 1.4 — introduce root_mean_squared_error()
#   scikit-learn 1.2 — OneHotEncoder(sparse_output=...) sustituye a sparse=
#   pandas 2.0       — API de agregación y tipos usada en los cuadernillos
#
# Probado de punta a punta con las versiones de 2026:
#   pandas 3.0 · numpy 2.4 · scikit-learn 1.9 · matplotlib 3.11 · statsmodels 0.15

pandas>=2.0
numpy>=1.24
scikit-learn>=1.4
matplotlib>=3.7
seaborn>=0.13
plotly>=5.18
statsmodels>=0.14
openpyxl>=3.1
jupyter>=1.0
